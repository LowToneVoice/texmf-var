\RequirePackage{tikz}
\usetikzlibrary{decorations.markings}

% Define a central arrow style with optional label
\tikzset{
    ->-/.style={
        postaction={
            decorate,
            decoration={
                markings,
                mark=at position 0.5 with {\arrow{>}}
            }
        }
    },
    ->- label/.style n args={2}{
        ->-,
        postaction={
            decorate,
            decoration={
                markings,
                mark=at position 0.5 with {
                    \arrow{>};
                    \node[#1] at (0,0) {#2};
                }
            }
        }
    }
}

% Usage example (in your document):
% \begin{tikzpicture}
%   \draw[mid arrow] (0,0) -- (3,0);
%   \draw[mid arrow label={above}{Label}] (0,1) -- (3,1);
% \end{tikzpicture}